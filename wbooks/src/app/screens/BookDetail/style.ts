import { StyleSheet } from 'react-native';
import { aliceBlue, black } from '@constants/colors';

const style = StyleSheet.create({
  bookDetailContainer: {
    display: 'flex',
    paddingVertical: 15,
    alignItems: 'center',
    paddingHorizontal: 15,
    backgroundColor: aliceBlue,
    height: '100%',
    color: black
  }
});

export default style;
